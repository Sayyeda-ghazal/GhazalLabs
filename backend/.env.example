PORT=5000
# Comma-separated list of allowed frontend origins (your Netlify URL)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:5500,https://your-site.netlify.app

# Optional: send contact form submissions to your email (Gmail: use an App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
CONTACT_TO=
