{
  "name": "ghazal-labs",
  "private": true,
  "description": "Ghazal Labs landing page + Netlify Functions",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "nodemailer": "^10.0.10"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}
